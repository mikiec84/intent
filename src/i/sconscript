Import('env')
Import('core')
Import('jsoncpp')
Import('zmq')

libs = [core, zmq, jsoncpp, 'pthread']
import platform
if platform.uname()[0] != 'Darwin':
    libs.append('rt')

prog = env.Program(target='i', source=Glob('*.cpp'), LIBS=libs)

Return('prog')

