#include "core/net/name_resolver.h"

#include "gtest/gtest.h"

using namespace intent::core::net;

TEST(name_resolver_test, localhost_synchronous) {
    name_resolver dns;
    //hostent host;
    //ASSERT_TRUE(dns.get_host_by_name("localhost", ipv4 | ipv6, host));
}
