import os

Import('env')

def run_here(cmd):
	return 'pushd .; cd src/external/zmq; %s; popd' % cmd

# Hack to do an autotools build of zmq as part of our scons build
zmq = env.Command('#build/zmq/libzmq.a', '', 
	'python src/external/zmq/build_as_needed.py')

Return('zmq')
